{% extends 'base.html.twig' %}

{% block title %}Questionnaires{% endblock %}

{% block body %}
    <h1>Questionnaires</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ questionnaire.id }}</td>
            </tr>
            <tr>
                <th>Form_code</th>
                <td>{{ questionnaire.formCode }}</td>
            </tr>
            <tr>
                <th>Consigne</th>
                <td>{{ questionnaire.consigne }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_questionnaires_index') }}">back to list</a>

    <a href="{{ path('app_questionnaires_edit', {'id': questionnaire.id}) }}">edit</a>

    {{ include('questionnaires/_delete_form.html.twig') }}
{% endblock %}
