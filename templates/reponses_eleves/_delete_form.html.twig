<form method="post" action="{{ path('app_reponses_eleves_delete', {'id': reponses_elefe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponses_elefe.id) }}">
    <button class="btn">Delete</button>
</form>
