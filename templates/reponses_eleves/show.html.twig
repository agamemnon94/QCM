{% extends 'base.html.twig' %}

{% block title %}ReponsesEleves{% endblock %}

{% block body %}
    <h1>ReponsesEleves</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponses_elefe.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ reponses_elefe.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponses_eleves_index') }}">back to list</a>

    <a href="{{ path('app_reponses_eleves_edit', {'id': reponses_elefe.id}) }}">edit</a>

    {{ include('reponses_eleves/_delete_form.html.twig') }}
{% endblock %}
