{% extends 'base.html.twig' %}

{% block title %}Reponses{% endblock %}

{% block body %}
    <h1>Reponses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ reponse.libelle }}</td>
            </tr>
            <tr>
                <th>Success</th>
                <td>{{ reponse.success ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponses_index') }}">back to list</a>

    <a href="{{ path('app_reponses_edit', {'id': reponse.id}) }}">edit</a>

    {{ include('reponses/_delete_form.html.twig') }}
{% endblock %}
